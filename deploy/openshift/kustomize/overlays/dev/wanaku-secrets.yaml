apiVersion: v1
kind: Secret
metadata:
  name: wanaku-service-credentials
type: Opaque
stringData:
  # OIDC client credentials secret for tool services (development)
  # IMPORTANT: Replace with your actual development secret
  # Consider using external secret management tools like Sealed Secrets or External Secrets Operator
  QUARKUS_OIDC_CLIENT_CREDENTIALS_SECRET: "replace-me-with-the-client-credentials-secret"
