mcp:
  tools:
    - send-message-to-rabbitmq:
        route:
          id: "rabbitmq-send"
        description: "{{tool.description}}"
        properties:
          - name: wanaku_body
            type: string
            description: The message to send to RabbitMQ
            required: true
