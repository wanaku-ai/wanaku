mcp:
  tools:
    - sql:
        route:
          id: "sql-tool"
        description: "Search on a database using a predefined query"
        properties:
          - name: wanaku_body
            type: string
            description: Input argument for the query
            required: true
