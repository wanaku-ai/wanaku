{
  "$schema" : "https://json-schema.org/draft-07/schema#",
  "properties" : {
    "podSecurityContext" : {
      "description" : "Pod-level security context for the operator Deployment.",
      "type" : "object",
      "properties" : {
        "runAsNonRoot" : {
          "description" : "Require the pod to run as a non-root user.",
          "type" : "boolean"
        },
        "seccompProfile" : {
          "type" : "object",
          "properties" : {
            "type" : {
              "description" : "The seccomp profile type (e.g. RuntimeDefault).",
              "type" : "string"
            }
          }
        }
      }
    },
    "containerSecurityContext" : {
      "description" : "Container-level security context for the operator container.",
      "type" : "object",
      "properties" : {
        "allowPrivilegeEscalation" : {
          "description" : "Whether the container can gain more privileges than its parent process.",
          "type" : "boolean"
        },
        "capabilities" : {
          "type" : "object",
          "properties" : {
            "drop" : {
              "description" : "Linux capabilities to drop.",
              "type" : "array",
              "items" : {
                "type" : "string"
              }
            }
          }
        }
      }
    },
    "app" : {
      "type" : "object",
      "properties" : {
        "livenessProbe" : {
          "type" : "object",
          "properties" : {
            "failureThreshold" : {
              "description" : "The failure threshold to use.",
              "type" : "integer"
            },
            "periodSeconds" : {
              "description" : "The period in which the action should be called.",
              "type" : "integer"
            },
            "timeoutSeconds" : {
              "description" : "The amount of time to wait for each action.",
              "type" : "integer"
            },
            "successThreshold" : {
              "description" : "The success threshold to use.",
              "type" : "integer"
            },
            "initialDelaySeconds" : {
              "description" : "The amount of time to wait before starting to probe.",
              "type" : "integer"
            },
            "httpGet" : {
              "type" : "object",
              "properties" : {
                "path" : {
                  "description" : "The http path to use for the probe.",
                  "type" : "string"
                },
                "scheme" : {
                  "description" : "The http schema to use for the probe.",
                  "type" : "string"
                }
              }
            }
          }
        },
        "imagePullPolicy" : {
          "type" : "string"
        },
        "image" : {
          "description" : "The container image to use.",
          "type" : "string"
        },
        "readinessProbe" : {
          "type" : "object",
          "properties" : {
            "failureThreshold" : {
              "description" : "The failure threshold to use.",
              "type" : "integer"
            },
            "timeoutSeconds" : {
              "description" : "The amount of time to wait for each action.",
              "type" : "integer"
            },
            "periodSeconds" : {
              "description" : "The period in which the action should be called.",
              "type" : "integer"
            },
            "successThreshold" : {
              "description" : "The success threshold to use.",
              "type" : "integer"
            },
            "initialDelaySeconds" : {
              "description" : "The amount of time to wait before starting to probe.",
              "type" : "integer"
            },
            "httpGet" : {
              "type" : "object",
              "properties" : {
                "path" : {
                  "description" : "The http path to use for the probe.",
                  "type" : "string"
                },
                "scheme" : {
                  "description" : "The http schema to use for the probe.",
                  "type" : "string"
                }
              }
            }
          }
        },
        "envs" : {
          "type" : "object",
          "properties" : {
            "QUARKUS_OPERATOR_SDK_CONTROLLERS_WANAKU_CAPABILITY_NAMESPACES" : {
              "type" : "string"
            },
            "QUARKUS_OPERATOR_SDK_CONTROLLERS_WANAKU_SERVICE_CATALOG_NAMESPACES" : {
              "type" : "string"
            },
            "QUARKUS_OPERATOR_SDK_CONTROLLERS_WANAKU_ROUTER_NAMESPACES" : {
              "type" : "string"
            }
          }
        },
        "startupProbe" : {
          "type" : "object",
          "properties" : {
            "failureThreshold" : {
              "description" : "The failure threshold to use.",
              "type" : "integer"
            },
            "periodSeconds" : {
              "description" : "The period in which the action should be called.",
              "type" : "integer"
            },
            "timeoutSeconds" : {
              "description" : "The amount of time to wait for each action.",
              "type" : "integer"
            },
            "successThreshold" : {
              "description" : "The success threshold to use.",
              "type" : "integer"
            },
            "initialDelaySeconds" : {
              "description" : "The amount of time to wait before starting to probe.",
              "type" : "integer"
            },
            "httpGet" : {
              "type" : "object",
              "properties" : {
                "path" : {
                  "description" : "The http path to use for the probe.",
                  "type" : "string"
                },
                "scheme" : {
                  "description" : "The http schema to use for the probe.",
                  "type" : "string"
                }
              }
            }
          }
        },
        "ports" : {
          "type" : "object",
          "properties" : {
            "http" : {
              "description" : "The http port to use for the probe.",
              "type" : "integer"
            }
          }
        }
      }
    }
  },
  "title" : "Values",
  "type" : "object"
}